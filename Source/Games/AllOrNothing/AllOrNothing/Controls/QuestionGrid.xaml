<UserControl
    x:Class="AllOrNothing.Controls.QuestionGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AllOrNothing.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:conv="using:AllOrNothing.Converters"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <conv:NullValueConverter x:Key="NullValueConverter"/>
    </UserControl.Resources>

    <Grid x:Name="MainGrid" 
          Background="{ThemeResource MainColor2}"
          CornerRadius="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <Grid Grid.Row="1">
            <Border Margin="10,10,10,10" 
                    Background="White"
                    CornerRadius="10">
                <TextBlock x:Name="QuestionTextBlock" Text="{x:Bind QuestionDisplay, Mode=OneWay}"
                           TextAlignment="Center" 
                           VerticalAlignment="Center" 
                           TextWrapping="WrapWholeWords" 
                           Style="{ThemeResource TitleStyle}"
                           />
            </Border>

        </Grid>


    </Grid>
</UserControl>
