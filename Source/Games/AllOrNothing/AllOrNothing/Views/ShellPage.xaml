<Page
    x:Class="AllOrNothing.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:AllOrNothing.Helpers"
    xmlns:behaviors="using:AllOrNothing.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:fa5="http://schemas.fontawesome.com/icons/"  
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    Loaded="OnLoaded"
    Style="{StaticResource PageStyle}"
    >

    <Page.Resources>
        <SolidColorBrush x:Key="NavigationViewTopPaneBackground" Color="{ThemeResource MainColor1}" />
        <SolidColorBrush x:Key="NavigationViewTopPaneForeground" Color="White" />
        <SolidColorBrush x:Key="NavigationViewItemForeground" Color="White"/>
        <!--<SolidColorBrush x:Key="NavigationViewItemForegroundPointerOver" Color="Red"/>-->
        <SolidColorBrush x:Key="NavigationViewItemForegroundSelected" Color="Red"/>
        <SolidColorBrush x:Key="NavigationViewItemForegroundSelectedPointerOver" Color="Red"/>
        <SolidColorBrush x:Key="NavigationViewItemForegroundPressed" Color="Red"/>
        <SolidColorBrush x:Key="NavigationViewItemForegroundSelectedPressed" Color="Red"/>
    </Page.Resources>
    <NavigationView
        x:Name="navigationView"
        IsBackButtonVisible="Collapsed"
        IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
        SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
        IsSettingsVisible="True"
        IsPaneToggleButtonVisible="True"
        ExpandedModeThresholdWidth="1280"
        PaneDisplayMode="Top"
        Foreground="White"
        Background="White">
        <NavigationView.MenuItems>
            <!--
            TODO WTS: Change the symbols for each item as appropriate for your app
            More on Segoe UI Symbol icons: https://docs.microsoft.com/windows/uwp/style/segoe-ui-symbol-font
            Or to use an IconElement instead of a Symbol see https://github.com/Microsoft/WindowsTemplateStudio/blob/release/docs/UWP/projectTypes/navigationpane.md
            Edit String/en-US/Resources.resw: Add a menu item title for each page
            -->
            <NavigationViewItem Content="Főmenü" helpers:NavHelper.NavigateTo="AllOrNothing.ViewModels.AllOrNothingViewModel" FontSize="{ThemeResource LargeFontSize}" FontWeight="bold" Foreground="White"/>
            <!--<NavigationViewItem>
                <NavigationViewItem.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Menüpont"/>

                        <Button Width="15" Height="15" Margin ="10,0,0,0" Padding="0,0,0,0"  Background="{ThemeResource SystemControlAccentAcrylicElementAccentMediumHighBrush}">
                            --><!--<StackPanel>
                                <Border/>
                                --><!--<SymbolIcon Symbol="Go"/>--><!--s
                                
                            </StackPanel>--><!--
                            <Image  Source="/Assests/cross3.png" Width="10" Height="10" Stretch="UniformToFill" />
                            --><!--<TextBlock Text="X"/>--><!--

                        </Button>
                       
                    </StackPanel>
                </NavigationViewItem.Content>
            </NavigationViewItem>-->
        </NavigationView.MenuItems>
        <!--<NavigationView.HeaderTemplate>
            <DataTemplate>
                <Grid>
                    <TextBlock
                        Text="{Binding}"
                        Style="{ThemeResource TitleTextBlockStyle}"
                        Margin="{StaticResource SmallLeftRightMargin}" />
                </Grid>
            </DataTemplate>
        </NavigationView.HeaderTemplate>
        <i:Interaction.Behaviors>
            <behaviors:NavigationViewHeaderBehavior
                DefaultHeader="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
                <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock
                                Text="{Binding}"
                                Style="{ThemeResource TitleTextBlockStyle}"
                                Margin="{StaticResource SmallLeftRightMargin}" />
                        </Grid>
                    </DataTemplate>
                </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
            </behaviors:NavigationViewHeaderBehavior>
        </i:Interaction.Behaviors>-->
        <Grid>
            <Frame x:Name="shellFrame" 
                   BorderThickness="0" 
                   IsNavigationStackEnabled="False"
                   Margin="15,15,15,15"/>
        </Grid>
    </NavigationView>
</Page>
